# GitHub Personal Access Token (required)
GITHUB_TOKEN=your_token_here

# Sync interval in minutes (default: 30, min: 5)
SYNC_INTERVAL_MINUTES=30

# Backend server port (default: 3001 for dev, 3000 for docker)
# For npm dev: typically 3001 (frontend uses 3750)
# For Docker: internal port 3000 (don't change)
PORT=3001

# Environment (development/production)
# Set to 'production' for building/deploying
NODE_ENV=development

# Log level (trace/debug/info/warn/error, default: info)
# LOG_LEVEL=info

# Database path (optional - defaults to ./data/repos.db)
# DATABASE_PATH=./data/repos.db
