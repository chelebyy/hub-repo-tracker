# GitHub Personal Access Token (required)
GITHUB_TOKEN=ghp_your_token_here

# Sync interval in minutes (default: 30)
SYNC_INTERVAL_MINUTES=30

# ===========================================
# PORT CONFIGURATION
# ===========================================

# Application port (default: 3750)
# Used by: Docker (nginx), npm dev (vite frontend)
PORT=3750

# Backend port for development mode (default: 3001)
# Only used when running frontend and backend separately
BACKEND_PORT=3001

